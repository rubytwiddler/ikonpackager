*icon order in grid.
*pack extension variation.
*exclude cursor theme directory for icon package selection.
*add no directory hierarchy type icons.
*bug error : couldn't create image from  ""

*progress bar when select folder.
*cancel behaviour.
*rename each/all size file.
